{
  "name": "functions-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "SET NODE_ENV=development&func start",
    "test": "SET NODE_ENV=development& ts-node src/api/operations.ts"
  },
  "dependencies": {
    "@azure/cosmos": "^3.14.1",
    "@azure/storage-blob": "^12.8.0",
    "dayjs": "^1.10.7",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "@azure/functions": "^1.2.3",
    "@types/node": "^16.10.1",
    "cross-env": "^7.0.3",
    "typescript": "^4.0.0"
  }
}
